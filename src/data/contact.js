// Contact — Section 8 (Final CTA + FAQ). Only the CTA's own copy lives here;
// phone/WhatsApp/email are NOT duplicated — they're reused directly from
// footer.js (see Contact/index.jsx), which is already the established single
// source of truth for contact details (nav.js reuses it the same way).
export const contact = {
  label: { index: '07', text: 'Start the Conversation' },
  headlineLines: ['Your journey', 'starts with a conversation.'],
  cta: { label: 'Plan your trip' },
};
